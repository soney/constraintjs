/*
var fsm = cjs.fsm()
				.add_state("a")
				.add_transition...


var constraint = cjs(fsm, {
	a: val1
	, b: val2
});


fsm .while_in("a", constraint1, constraint2)
	.while_in("

application.while_in("painting_mode", title.text("Paint"), md_constraint.active())



var fsm = cjs.fsm()
			.add_state("editing_text")
			.add_state("sliding")
			.add_state("idle")

//constraints: slider, text
slider = cjs.$("#slider");
text = cjs.$("#text");

text.val()

fsm.while_in("editing_text", slider.val(text.val()))
	.while_in("sliding")


var constraint = cjs(function() {
	return c1.get() + 5;
});

cjs.$("#text").text(constraint);


var c1 = cjs(1); //Variables
c1 = cjs(function() {
	return c2.get();
});

c1.set(2);

var c2 = cjs(function() {
	return c1.get() + 1;
});

c2 = c1.add(1);
*/
var c1 = cjs(1); //constraint
//binding: constraint -> dom
//
//state should be held in JS, not in the DOM

<input type="text" id="inp" />

c1 = $("#inp").val();
c2 = cjs(c1);
$("#inp").val(c1);

cjs.$("#inp") //Wrapper, constraint around dom value
cjs.dom

c1 = cjs(fsm, {state1: 1});


/*

getter: return inp.val;
//nullify whenever inp changes

fsm .while_in("editing_text", slider.val(text.val()))
	.while_in("sliding")
	*/
